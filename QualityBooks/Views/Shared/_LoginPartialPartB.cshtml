@using Microsoft.AspNetCore.Identity
@using QualityBooks.Models
@*Comment below if have error when access login b*@
@*@model QualityBooks.Models.ManageViewModels.IndexViewModel*@

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@if (SignInManager.IsSignedIn(User))
{

<ul class="nav navbar-nav navbar-right">
    <li> <img src="@Url.Action("GetUserPhoto", "Manage")" width="50" height="50" /></li>
    <li><a asp-controller="Manage" asp-action="Index" title="Manage"><span class="glyphicon glyphicon-user"></span> @UserManager.GetUserName(User)</a></li>
    <li><form asp-area="" asp-controller="Account" asp-action="Logout" method="post" id="logoutForm">
    <button type="submit" class="btn btn-link navbar-btn navbar-link">Log out</button> </form></li>
</ul>
    @*<div class="row LoggedUserName">
        <div class="col-sm-2">
            <div class="mid_username">
                <a asp-area="" asp-controller="Manage" asp-action="Index" title="Manage">@UserManager.GetUserName(User)</a>
            </div>
        </div>
        <div class="col-sm-2">
            <div class="mid_userphoto">
                <img src="@Url.Action("GetUserPhoto", "Manage")" width="50" height="50" />
            </div>
        </div>
        <div class="col-sm-2">
            <div>

                    <button type="submit" class="btn btn-link navbar-btn navbar-link">Log out</button>
                </form>
            </div>
        </div>
    </div>*@






}

